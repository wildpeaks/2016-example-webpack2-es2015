/* eslint-env node, mocha */
/* eslint-disable no-undefined */
import {strictEqual} from 'assert';
import {myconst, myfunction} from './';

describe('/examples/module1', () => {
	it('myconst', () => {
		strictEqual(myconst, 123);
	});
	it('myfunction', () => {
		strictEqual(myfunction(), 1111);
		strictEqual(myfunction(true), 1111);
		// strictEqual(myfunction(false), 1222); // Uncomment to change the coverage of "module1" to 100%
	});
});
