/* eslint-env node, mocha */
/* eslint-disable no-undefined */
import {strictEqual} from 'assert';
import {createParagraph, createImage} from './';

describe('/examples/module2', () => {
	it('createParagraph', () => {
		strictEqual(typeof createParagraph, 'function');
	});
	it('createImage', () => {
		strictEqual(typeof createImage, 'function');
	});
});
